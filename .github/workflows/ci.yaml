name: Run CI/CD Jobs

on:
  push:
    paths:
      - ".github/workflows/**.yml"
      - "**/Cargo.toml"
      - "**.rs"
  schedule:
    - cron: '0 16 * * WED'

jobs:
  test:
    name: Run Tests
    uses: ./.github/workflows/test.yaml
    secrets: inherit

